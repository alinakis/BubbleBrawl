// Slight vertical oscillation (up and down)
y += sin(current_time/50) * 0.5;  // Adjust for more or less movement

// Slight horizontal oscillation
x += cos(current_time/50) * 0.5;  // Adjust for more or less movement